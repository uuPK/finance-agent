# Finance Agent Windows 部署与验收指南

本文用于在全新的 Windows 10/11 64 位电脑上，从 GitHub 克隆项目、导入赛事官方脱敏数据、生成 67 题评测基线并启动系统。所有命令在 **PowerShell** 执行；示例将项目放在 `D:\finance-agent`，官方数据包放在 `D:\contest-data\htsc`。

> 本项目只使用赛事官方脱敏数据。不要使用已删除的合成数据脚本，也不要把官方 CSV、`Q&A.xlsx`、`.env` 或任何客户数据提交到 Git。

## 1. 部署后的服务与端口

| 服务 | 地址 | 作用 |
| --- | --- | --- |
| 前端 | `http://127.0.0.1:5173` | 智能问数、历史、元数据、评测中心 |
| 后端 | `http://127.0.0.1:8000` | FastAPI、SSE、导出、评测 API |
| PostgreSQL 16 | `127.0.0.1:5432` | 官方业务数据、元数据、审计与评测记录 |
| 模型服务 | `https://api.deepseek.com` | QueryPlan、SQL 与语义审核（默认） |

前端开发服务器已将 `/api` 自动代理至后端，因此本地不需要额外配置前端 API 地址。

## 2. 安装前置软件

需要可访问互联网并具备安装权限。安装 Git、Docker Desktop、Node.js LTS 和 uv：

```powershell
winget install --id Git.Git --exact --source winget
winget install --id Docker.DockerDesktop --exact --source winget
winget install --id OpenJS.NodeJS.LTS --exact --source winget
winget install --id astral-sh.uv --exact --source winget
```

Docker Desktop 首次启动若提示 WSL 2，使用管理员 PowerShell 执行并重启：

```powershell
wsl --install
```

关闭并重新打开 PowerShell，确认工具可用；再由 uv 安装项目使用的 Python 3.13：

```powershell
git --version
docker --version
uv --version
node --version
npm --version
uv python install 3.13
```

启动 Docker Desktop，确认其状态为 `Engine running`。如果系统盘空间紧张，可在 Docker Desktop 的 `Settings -> Resources -> Advanced` 中将 `Disk image location` 改到 D 盘后点击 `Apply & restart`。

## 3. 获取项目与配置模型密钥

```powershell
Set-Location D:\
git clone https://github.com/uuPK/finance-agent.git finance-agent
Set-Location D:\finance-agent
Copy-Item .env.example .env
notepad .env
```

在 `.env` 至少填入可用的 DeepSeek 密钥；不要加引号、空格或换行：

```env
LLM_PROVIDER=deepseek
LLM_MODEL=deepseek-chat
LLM_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=your_deepseek_api_key
```

其余默认值可保持不变。公司网络需要代理时，再填写 `LLM_PROXY_URL=http://127.0.0.1:7890`；不使用代理则保持空值。修改 `.env` 后必须重启后端。

## 4. 准备赛事官方数据包

从赛事组织方获得官方压缩包并解压到本机，例如 `D:\contest-data\htsc`。该目录必须直接包含：

- `表描述.sql`
- `Q&A.xlsx`
- 8 个 UTF-8 CSV 文件，分别匹配：
  `ads_cust_info_d_*.csv`、`dim_branch_*.csv`、`dim_product_*.csv`、`dim_public_*.csv`、
  `dwd_cust_hold_d_*.csv`、`dwd_cust_tran_d_*.csv`、`dws_cust_aset_d_*.csv`、`dws_cust_fin_d_*.csv`

不要把这些文件放进仓库；`data/` 目录只保存接入说明。

## 5. 初始化 PostgreSQL、官方数据与 67 题基线

以下为**新机器首次部署**的完整步骤。`schema.sql` 已包含当前表结构；新库不需要逐个执行旧的 `002` 至 `005` 迁移文件。

### 5.1 启动数据库并创建表结构

在项目根目录执行：

```powershell
Set-Location D:\finance-agent
docker compose up -d postgres
docker compose ps
```

等待 `finance-agent-postgres` 显示 `healthy` 后执行：

```powershell
Get-Content -Raw .\backend\db\schema.sql |
  docker exec -i finance-agent-postgres psql -U finance_agent -d finance_agent -v ON_ERROR_STOP=1
```

### 5.2 导入官方数据和官方原始问答

```powershell
Set-Location D:\finance-agent\backend
uv sync --extra dev --frozen
uv run python .\db\load_official_dataset.py --data-dir "D:\contest-data\htsc"
```

导入器会验证 8 个 CSV 与 `Q&A.xlsx`，清理旧的官方基线数据，写入 8 张 `mart` 官方表、AI 友好元数据和 7 条原始官方问答。源文件不会被复制进 Git 仓库。

### 5.3 生成 60 条官方派生回归题

```powershell
uv run python .\db\load_official_benchmark_cases.py
```

该命令在已导入的官方表上实际执行标准 SQL，刷新 `REG-001` 至 `REG-060`，并保留 7 条 `OFFICIAL-*` 原始问答，形成 67 条激活评测案例。它不会生成业务模拟数据。

### 5.4 验收数据导入

```powershell
docker exec finance-agent-postgres psql -U finance_agent -d finance_agent -c "select count(*) as active_cases from evaluation.eval_cases where is_active;"
docker exec finance-agent-postgres psql -U finance_agent -d finance_agent -c "select source_type, count(*) from evaluation.eval_cases where is_active group by source_type order by source_type;"
```

预期第一条输出为 `67`；第二条应包含 `official` 7 条和 `official_derived` 60 条。

### 5.5 已部署旧版本的升级说明

如果现有数据库已经是官方数据，但缺少新语义元数据，可在备份后执行以下两个**仅更新元数据**的迁移：

```powershell
Get-Content -Raw .\db\migrations\007_strengthen_official_semantics.sql |
  docker exec -i finance-agent-postgres psql -U finance_agent -d finance_agent -v ON_ERROR_STOP=1

Get-Content -Raw .\db\migrations\008_add_average_total_asset_semantics.sql |
  docker exec -i finance-agent-postgres psql -U finance_agent -d finance_agent -v ON_ERROR_STOP=1
```

如果旧库来自已经废弃的合成数据版本，不建议套用 `006` 迁移。请先备份并清理本地 Docker 卷，再按本节从官方数据包完整初始化。

## 6. 安装前端依赖

打开第二个 PowerShell 窗口：

```powershell
Set-Location D:\finance-agent\frontend
npm ci
```

`npm ci` 严格使用 `package-lock.json`，首次安装可能需要数分钟。

## 7. 启动后端和前端

保留两个终端窗口，不要关闭正在运行的服务。

### 窗口 A：后端

```powershell
Set-Location D:\finance-agent\backend
uv run uvicorn app.main:app --host 127.0.0.1 --port 8000
```

另开一个窗口进行健康检查：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/health
Invoke-RestMethod http://127.0.0.1:8000/api/llm/status
```

第一条应返回 `status: ok`；第二条应显示 LLM 已启用，且不返回密钥。

### 窗口 B：前端

```powershell
Set-Location D:\finance-agent\frontend
npm run dev -- --host 127.0.0.1 --port 5173
```

访问 <http://127.0.0.1:5173>。可先输入：

```text
2026年第一季度，客户交易金额合计是多少？
```

页面会显示元数据检索、QueryPlan、审核/修复、SQL 执行与结果验证阶段。模型调用耗时和结果取决于网络、API 额度与当前模型版本。

## 8. 运行评测与质量检查

### 8.1 本地代码质量检查

```powershell
Set-Location D:\finance-agent\backend
uv run ruff check app tests
uv run pytest

Set-Location D:\finance-agent\frontend
npm run lint
npm run build
```

### 8.2 67 题真实回归评测

在浏览器打开“评测中心”，选择完整评测并发起运行。系统逐题调用当前 Agent，结果会记录在 `evaluation.eval_runs` 和 `evaluation.eval_results` 中；仪表盘展示真实的可执行率、结果准确率、一次通过率、修复后通过率、耗时和失败原因。

> 全量评测会调用模型服务并消耗额度。不要把“67 条标准 SQL 可执行”误报为模型实际通过率；是否达到赛题准确率目标应以该次运行的真实结果为准。

## 9. 停止、更新与重启

停止前后端：分别在运行窗口按 `Ctrl+C`。

停止数据库但保留本地数据：

```powershell
Set-Location D:\finance-agent
docker compose stop postgres
```

日常重启：

```powershell
Set-Location D:\finance-agent
docker compose up -d postgres

# 新窗口 A
Set-Location D:\finance-agent\backend
uv run uvicorn app.main:app --host 127.0.0.1 --port 8000

# 新窗口 B
Set-Location D:\finance-agent\frontend
npm run dev -- --host 127.0.0.1 --port 5173
```

更新代码后执行：

```powershell
Set-Location D:\finance-agent
git pull --ff-only

Set-Location .\backend
uv sync --extra dev --frozen

Set-Location ..\frontend
npm ci
```

若更新说明包含数据库迁移，先完成迁移再重启后端；不要在不确认影响的情况下执行 `006_replace_synthetic_with_official_dataset.sql`。

只有确定删除全部本地数据库及其审计/评测记录时，才执行：

```powershell
Set-Location D:\finance-agent
docker compose down -v
```

执行后必须从第 5 节重新初始化。

## 10. 常见故障

### PostgreSQL 未启动或无法连接

```powershell
Set-Location D:\finance-agent
docker compose ps
docker compose logs postgres --tail 100
```

确认 Docker Desktop 为 `Engine running`。若 `5432` 被占用，可修改 `docker-compose.yml` 的宿主机端口，并同步修改 `.env` 的 `DATABASE_URL`。

### 官方数据导入失败

确认 `--data-dir` 指向解压后的顶层目录，且目录中同时存在 `表描述.sql`、`Q&A.xlsx` 和 8 个 UTF-8 CSV。导入前不要手工修改官方字段名；报错时优先修正数据包路径、编码或缺失文件，而不是修改业务表结构。

### 前端能打开，但 API 报错或一直转圈

确认后端仍运行，并执行：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/health
```

前端应使用 `5173`、后端应使用 `8000`，以匹配 Vite 代理和 CORS 默认配置。

### 模型调用失败

依次检查：

1. `.env` 中 `DEEPSEEK_API_KEY` 已填写且无引号、空格或换行。
2. `http://127.0.0.1:8000/api/llm/status` 显示已启用 LLM。
3. 当前网络可访问 `https://api.deepseek.com`；受限网络需正确设置 `LLM_PROXY_URL`。
4. API Key 额度、权限及模型服务状态正常。

### 端口被占用

```powershell
Get-NetTCPConnection -LocalPort 5432,8000,5173 -ErrorAction SilentlyContinue |
  Select-Object LocalPort, State, OwningProcess
```

结束确认不再需要的进程，或同时调整 Docker 端口映射、`.env`、前端代理和 CORS。建议开发机保持默认端口，减少配置差异。

# Data Directory

此目录包含经项目维护方确认可公开的赛事官方脱敏数据包，位于
`official/htsc/`，另有对应的 SHA-256 清单。该数据包可与仓库代码一同克隆，
从而重建本项目的业务表和评测基线。

不得提交真实客户数据、未经确认可公开的数据、.env、API 密钥、数据库卷、审计日志
或模型调用/评测运行记录。正式数据由 `backend/db/load_official_dataset.py` 导入；
应用对客户姓名字段保持敏感字段拦截。
